{
  "name": "riftbound-sign-in-lambda",
  "version": "1.0.0",
  "description": "Sign-in Lambda function for Riftbound Online",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "bundle": "npm run build && zip -r ../sign_in.zip dist node_modules package.json"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.2.2"
  }
}
