# CDK Deployment Configuration
# Copy this to .env and customize for your environment

# ========================================
# Environment
# ========================================
ENVIRONMENT=dev

# AWS Region
AWS_REGION=us-east-1

# ========================================
# ECS Configuration
# ========================================

# Docker image URL (must be accessible)
# For development: any public image (nginx:latest, node:18, etc)
# For production: use ECR image
CONTAINER_IMAGE=nginx:latest

# ECS Task Configuration
# CPU: 256, 512, 1024, 2048, 4096
TASK_CPU=1024

# Memory: depends on CPU (512-30720, in 1GB increments)
TASK_MEMORY=2048

# Desired number of tasks
DESIRED_COUNT=2

# ========================================
# Cognito Configuration
# ========================================

# User Pool name (auto-generated based on environment)
# COGNITO_USER_POOL_NAME=riftbound-dev-pool

# Client ID (output from CDK)
# COGNITO_CLIENT_ID=<generated>

# ========================================
# DynamoDB Configuration
# ========================================

# Billing mode: PAY_PER_REQUEST or PROVISIONED
# Currently set to on-demand (PAY_PER_REQUEST)

# Point in time recovery
ENABLE_POINT_IN_TIME_RECOVERY=true

# ========================================
# AWS Credentials (if not using ~/.aws/credentials)
# ========================================
# AWS_ACCESS_KEY_ID=your_key
# AWS_SECRET_ACCESS_KEY=your_secret
# AWS_SESSION_TOKEN=your_token (if using temporary credentials)

# ========================================
# Development
# ========================================
LOG_LEVEL=info
NODE_ENV=development
